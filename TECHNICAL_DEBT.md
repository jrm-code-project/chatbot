# Technical Debt Register

This document prioritizes the highest-value technical debt currently visible in the `chatbot` codebase. It focuses on debt that increases change risk, operational fragility, or maintenance cost.

This register was refreshed again on **2026-07-07** after a repo scan, test-system load, and review of the recent Google cache and sandbox-persona isolation fixes.

It now also incorporates the later snapshot that was mistakenly written to `TECHNICAL-DEBT.md-`, so the current file reflects both the longer-running debt register and the most recent repo-shape findings.

## Priority scale

- **P0** — high-risk architectural or operational debt that can cause broad regressions
- **P1** — important maintainability debt that slows feature work or makes bugs likely
- **P2** — worthwhile cleanup that should follow once higher-risk items are under control

## Prioritized debt

| Priority | Area | Debt | Evidence | Why it matters | Recommended direction |
| --- | --- | --- | --- | --- | --- |
| **P0** | Runtime context and configuration | **Legacy ambient globals still coexist with the newer runtime-context model.** | The bridge is narrower now: deprecated ambient aliases now live in `src/core/runtime-compatibility.lisp` instead of being interleaved throughout `src/core/vars.lisp`; `current-default-conversation` now treats the default runtime context as canonical without importing raw `*default-conversation*` state into it; `setf current-default-conversation` also now updates only the canonical runtime context instead of mirroring back into the deprecated `*default-conversation*` special; the generic context-owned helpers for MCP config, shared startup state, auto-init policy, logging, HTTP timeouts, and agentic-loop defaults also now update only the runtime context instead of writing through to legacy specials; the transient active-conversation/planner helpers also now read and write only runtime-context state instead of consulting or mirroring their legacy globals; the function-seam and approval helpers now also read and write only runtime-context state instead of falling back to ambient legacy specials when called without an explicit context; explicit `call-with-runtime-context` execution no longer rebinds or mirrors the legacy `*default-conversation*` special and instead relies on runtime-context accessors directly; no-arg function seams now honor an active explicit runtime context instead of ambient specials; approval seams now also stay owned by the runtime context instead of being overridden or synced through legacy approval specials during default-context execution; unrelated runtime-context resolution paths such as `new-chat`, persona runtime-context shaping, startup-chatbot initialization, agentic-loop fallback, and public chat entry-context selection no longer sync legacy `*default-conversation*` into the canonical runtime context; runtime function-seam and approval helpers also no longer sync legacy `*default-conversation*` just to read or write unrelated seams; non-chat `call-with-runtime-context` opt-out paths also no longer persist legacy function/approval seam overrides back into the canonical default runtime context; public `chat` entry now also re-enters the resolved conversation's runtime context before running the turn, so ambient default-conversation chats no longer execute under an unrelated default context; bot tool execution now also enters the bot's runtime context before invoking built-in or MCP tools; and the old generic function-seam mirroring layer is gone. Remaining debt is the deprecated alias surface itself and any external callers that still mutate those specials instead of constructing explicit runtime contexts. | This keeps behavior dependent on ambient mutable state and compatibility mirroring, which makes bugs non-local and complicates concurrent or nested execution. The main loop is cleaner than before, and the compatibility surface is more explicit, but the bridge still exists. | Finish the runtime-context migration: stop adding new legacy-global readers, convert remaining compatibility-only no-context/default-context fallback paths to explicit runtime contexts, then continue shrinking or removing the dedicated compatibility module. |
| **P0** | MCP lifecycle | **Subprocess and thread lifecycle management is still fragile, even after recent shutdown hardening.** | `src/mcp/mcp-lifecycle.lisp` now explicitly tracks stderr threads, prefers stream closure plus graceful process termination before escalation, cleans up launched subprocesses when supervision startup fails partway through, now routes shutdown orchestration through focused helpers for stream closure, supervision-thread stop, process stop, and final state clearing, and isolates the forced thread/process termination fallback behind dedicated helpers with deterministic tests for graceful-vs-force process shutdown. `src/mcp/mcp.lisp` now also aborts pending request mailboxes when the reader loop exits unexpectedly, so an EOF or reader failure fails in-flight callers immediately instead of making them wait for timeout. `src/mcp/mcp-startup.lisp` is also thinner than before: startup-entry definition validation, server launch/cleanup, success/failure shaping, server extraction, empty-status application, shared startup-bot creation/reuse, shared-startup reference resolution, shared-server ownership checks, owned-server shutdown, final startup-reference cleanup, and config/status/apply/log strict-startup policy now route through focused helpers; shutdown also now clears stale startup status instead of leaving a stopped startup bot looking reusable, strict required-server startup failures now tear down already-started shared servers instead of leaking orphaned MCP processes or preserving incompatible shared startup state, and `startup-chatbot-initialized-p` now reports completed startup state instead of mere startup-bot object presence. Remaining debt is that startup policy and shutdown fallback still sit close to low-level lifecycle mechanics, and forced thread/process termination remains the last-resort path. | This is the highest operational-risk code in the repo: process teardown races, blocked I/O, pending-call hangs, and partial startup failures are hard to reproduce and easy to break. The new lifecycle tracking lowers immediate shutdown, unexpected-reader-exit, partial-startup, stale-shutdown-state, strict-startup-leak, and ambiguous-startup-state hazards, but startup policy is still coupled to low-level lifecycle mechanics. | Keep splitting lifecycle into smaller layers: command/environment resolution, process launch, reader supervision, startup orchestration, and shutdown policy. Preserve the tracked-thread/graceful-first shutdown path, but isolate the remaining forced-termination fallback and shared-startup policy behind smaller lifecycle boundaries. |
| **P1** | Chat orchestration | **Top-level chat flow is narrower, but dispatch/preparation and entry-shell routing still span multiple core seams.** | `src/core/chat.lisp` now mainly holds `dispatch-chat-turn` and `chat-turn`, `src/core/chat-routing.lisp` owns conversation requirement plus planner routing, and `src/core/chat-entry.lisp` owns public entry context resolution plus active-conversation shell binding. Post-turn compression/checkpoint policy still routes through `finalize-chat-turn-result` in `src/core/conversations.lisp`. | The pure-turn refactor improved this area, and the public entry and routing shells are now separated, but high-traffic chat behavior still spans several core files and retains compatibility policy nearby. Any future change to routing or provider dispatch still touches central orchestration code. | Continue the shell/core split: keep post-turn policy in shared helpers and extract remaining compatibility-only concerns into smaller layers so public chat orchestration becomes mostly delegation. |
| **P1** | Provider backends | **Provider turn submission functions remain stateful and mixed-responsibility, though the main tool-recursion clusters are now narrower across all three providers.** | `src/backends/backend-google.lisp`, `src/backends/backend-openai.lisp`, and `src/backends/backend-gemini.lisp` now each route request submission and tool-recursion shaping through named helper layers above their provider loop entry points. | Streaming parsing, payload building, retry policy, tool-call detection, tool-recursion shaping, usage extraction, and final-outcome normalization still live close together in a few provider modules. That raises regression risk for tool-calling and provider edge cases even after the inline clusters are reduced. | Keep extracting provider-specific phases into smaller helpers: request assembly, response parsing, tool-call extraction, retry decision, tool-recursion shaping, and outcome normalization. Preserve `run-provider-turn-loop` as the shared orchestration backbone. |
| **P1** | Tool execution and orchestration | **The MCP tool execution layer is still too large and central.** | `src/mcp/tool-execution.lisp` spans builtin argument normalization, filesystem policy, planner/minion orchestration, and the main `execute-chatbot-tool` dispatch. | This file has become the coordination hub for many unrelated concerns. That makes every new tool or orchestration change riskier and harder to review. | Split by concern: builtin validation/coercion, filesystem tools, persona/system-instruction tools, planner/minion tools, and shared execution plumbing. |
| **P1** | Conversation restoration and personas | **Conversation construction still carries a lot of policy and side effects.** | `src/core/conversations.lisp`: `new-chat-persona`, `restore-minions`, `summarize-old-history`, `prune-conversation-context-if-needed`. `src/personas/personas.lisp` still mixes pure shaping with background compression and MCP-server attachment flow. | Recent refactors made these paths more functional, but persona loading and minion restore still combine configuration, state shaping, startup side effects, and persistence behavior. | Keep peeling apart planning from effects: separate persona config resolution, preload shaping, restore planning, and side-effect application into distinct helpers/modules. |
| **P1** | Checkpoint restore semantics | **Checkpoint restore is more persona-aware now, but the restore schema still does not capture the full conversation-construction contract.** | The shared checkpoint schema now persists both runtime worker identity and source persona identity, and persona-backed restore now routes back through `new-chat-persona` so persona config, memory preload, MCP attachment, and filesystem allowlist state are reconstructed before persisted history is reapplied. Remaining gaps are that restore still relies on conversation constructors plus post-restore patch-up, and not every constructor input or side-effect policy is yet modeled explicitly in the persisted schema. | The biggest false-identity bug is reduced: restored minions no longer just *look* like persona workers while silently losing persona-owned behavior. The remaining debt is structural: save/restore still depends on partially implicit construction behavior, which can drift as constructors evolve. | Keep moving toward an explicit restore contract: persist the remaining constructor inputs and recovery policy decisions that materially affect runtime behavior, so save/restore can consume one normalized schema with less post-construction patch-up. |
| **P1** | Persona startup and recovery policy | **`new-chat-persona` now mixes normal persona construction with debugger-driven recovery and restore fallback policy.** | `src/personas/personas.lisp`: `resolve-persona-directory` and `signal-persona-directory-not-found` now own restart-based recovery policy. `src/core/conversations.lisp`: `new-chat-persona` branches between full persona startup and a fallback plain `new-chat` path when a missing persona is skipped. | The recovery behavior is practical, but the main persona constructor now decides both *how* personas are loaded and *what to do when they cannot be loaded*. That makes behavior dependent on restart choices and makes restore semantics less obvious to callers. | Split persona lookup/recovery planning from conversation construction. Keep `new-chat-persona` focused on the happy path, and move “create / redirect / skip restore” policy into a dedicated restore-or-resolution layer. |
| **P1** | Persona memory storage format | **Persona memory loading is accumulating compatibility heuristics instead of enforcing one canonical schema.** | `src/personas/personas.lisp`: `persona-memory-json-records`, `parse-persona-memory-jsonl-records`, `persona-memory-record-type-name`, and `ensure-persona-memory-server-storage-format` now accept graph JSON, JSONL records, embedded graph objects, inferred type-less records, and empty records. | This keeps old personas working, but every new edge case adds more implicit parsing rules to a critical startup path. Over time that increases the chance of silent misclassification, surprising migrations, and hard-to-debug persona-specific failures. | Define a versioned on-disk schema for persona memory, pick one canonical write format, and funnel old files through explicit one-time migrations instead of growing the permissive parser indefinitely. |
| **P2** | Duplicate code trees | **The repository contains a mirrored sandbox code tree that can drift from `src/`.** | `minion-sandbox-Gopher\` contains 24 tracked files, while `src\` contains 39 tracked files. The mirrored tree duplicates major runtime modules such as `core\chat.lisp`, `mcp\tool-execution.lisp`, and backend files. | Near-copies multiply maintenance cost and create a real risk of fixing behavior in one tree but not the other. | Either generate the sandbox tree from a single source, replace it with fixtures/test artifacts, or delete it if it is no longer required. |
| **P2** | Runtime artifacts in the repository | **Legacy tracked minion state files still exist in git, even though live runtime writes no longer default there.** | `data/minions\` still contains tracked JSON state files such as `Planner.json`, `Gopher.json`, `Documentation.json`, and others. `save-minion-state` in `src/core/conversations.lisp` now defaults to `CHATBOT_MINIONS_DATA_DIR` when set, otherwise `~/.chatbot/data/minions/`, so new live writes no longer target the repository by default. | The risk is reduced because ordinary runtime activity no longer churns tracked files, but the repo still contains legacy state artifacts that can confuse ownership and review boundaries. | Remove or relocate the remaining tracked state files, or reclassify a minimal subset as intentional fixtures in a dedicated test-data area. |
| **P2** | Test coverage shape | **The highest-risk lifecycle and concurrency paths appear less directly exercised than core request/response behavior.** | The suite is broad (`tests/tests-*.lisp`), but the riskiest areas are teardown races, background thread/process cleanup, and shared-state interaction across runtime contexts. | The code most likely to fail in production is not the easiest to validate with ordinary unit tests. Without stronger lifecycle tests, regressions may only appear under real runtime conditions. | Add focused tests around MCP shutdown, thread cleanup, and recovery behavior, ideally with deterministic seams already present in `vars.lisp`. |

## Additional current-state findings

These items came from the later debt snapshot and sharpen the operational picture of the repository as it exists today.

| Priority | Area | Debt | Evidence | Why it matters | Recommended direction |
| --- | --- | --- | --- | --- | --- |
| **P0** | Source layout | **Core orchestration logic is still concentrated in a few very large files.** | `src/orchestration/agentic-loops.lisp` (~50 KB), `src/core/conversations.lisp` (~42 KB), `src/core/vars.lisp` (~36 KB), and `src/core/data.lisp` (~31 KB). | Cross-cutting changes still land in a small set of hotspot files, which raises merge pressure and regression risk. Recent loop/compression/logging work had to touch several of these files together. | Split by lifecycle phase and concern: agentic-loop startup vs. supervision vs. monitor behavior; conversation construction vs. compression vs. checkpoint persistence; runtime-context accessors vs. compatibility behavior. |
| **P0** | Persistence model | **Checkpoint and minion-state persistence is centralized more than before, but restore still depends on constructor-specific patch-up after schema decode.** | The shared checkpoint schema now drives both checkpoint restore and minion restore, including explicit runtime-vs-source persona identity. Remaining manual work still lives in `src/core/conversations.lisp`, where decoded restoration data is routed through `new-chat` or `new-chat-persona` and then patched with persisted conversation state such as history, interaction id, cached-content fields, and adaptive pruning state. | Centralizing the wire format reduces drift, but persisted-field coverage is still only as reliable as the post-construction patch-up logic. Constructor changes can still create partial restore regressions if the schema-to-runtime mapping is not kept fully aligned. | Keep shrinking the post-decode patch-up layer: move more constructor-relevant runtime fields into explicit shared restoration helpers so checkpoint restore and minion restore depend on one normalized schema-to-runtime mapping. |
| **P1** | Duplicate code trees | **The mirrored sandbox tree is still an active maintenance risk.** | `minion-sandbox-Gopher\` contains tracked copies of major runtime modules such as `core\chat.lisp`, `core\conversations.lisp`, `mcp\tool-execution.lisp`, and backend files. | Duplicate trees multiply maintenance cost and make it easy to fix one path while forgetting the other. | Either generate the sandbox tree from `src\`, shrink it to true fixtures only, or remove it if it is no longer required. |
| **P1** | Build cleanliness | **Routine compilation still emits avoidable warning noise.** | `tests/tests-mcp.lisp:2293-2299` still calls `new-chat` with `:content-cache-ttl`, while `src/core/conversations.lisp:100-123` now accepts `:content-cache-ttl-seconds`. `tests/tests-mcp.lisp:2318-2324` still asserts against `chatbot-content-cache-ttl`, while `src/core/data.lisp:186-188` defines `chatbot-content-cache-ttl-seconds`. Loading `chatbot/tests` currently emits the resulting style warnings. | Persistent warning noise hides real regressions and makes build output harder to trust. It also means parts of the test suite are still validating stale APIs instead of the current public surface. | Make warning cleanup a standing rule: fix stale test call sites and assertions promptly, remove or explicitly ignore unused bindings, and keep regular test/build output warning-light. |
| **P1** | Test structure | **Several test files are large enough to be hard to evolve safely.** | `tests/tests-runtime.lisp` (~121 KB), `tests/tests-mcp.lisp` (~116 KB), and `tests/tests-personas.lisp` (~90 KB). | Large integration-heavy test files make it harder to find coverage gaps, isolate failures, or add narrowly scoped regressions. | Split test files by behavior slice rather than subsystem umbrella, especially around runtime context, MCP lifecycle, persona loading, and checkpoint restore semantics. |

## Verified 2026-07-07 refresh findings

These are the most important debt surfaces confirmed during the latest pass.

| Priority | Area | Debt | Evidence | Why it matters | Recommended direction |
| --- | --- | --- | --- | --- | --- |
| **P0** | Checkpoint identity and state isolation | **Checkpoint identity still depends on persona metadata being stamped correctly on each conversation.** | `src/core/conversations.lisp:255-284` persists conversations under `chatbot-persona-name`, falling back to `"DefaultConversation"` when the bot lacks a persona name. `src/orchestration/sandbox-personas.lisp:145-169` now has to set `(chatbot-persona-name bot)` explicitly during spawn so sandbox personas checkpoint separately. | This is a brittle coupling between runtime identity and persistence. A missed metadata assignment can silently redirect persistence to the shared default checkpoint name, which is exactly the kind of cross-persona bleed that was just fixed. | Move checkpoint identity into an explicit persistence identifier set at conversation construction time, validate it before saving, and stop relying on incidental persona-name metadata as the persistence key. |
| **P1** | Provider error normalization | **Google cached-content error handling still depends on provider-specific message parsing instead of a normalized error layer.** | `src/backends/google-caching.lisp:307-343` now has helper predicates that string-match `"CachedContent not found"` and `"PERMISSION_DENIED"` in either returned bodies or raised conditions in order to treat missing caches as benign replacement misses. | The current fix is correct, but it exposes a wider debt surface: backend behavior depends on Dexador condition text and Google-specific phrasing. That is hard to reason about, easy to regress, and likely to repeat in other backend error paths. | Add a normalized HTTP/provider error classification layer in `http-utils` or backend helpers so provider code consumes structured failure reasons instead of parsing human-readable error text. |
| **P1** | Build and test hygiene | **The test system still compiles with known warning-producing API drift.** | `tests/tests-mcp.lisp:2293-2299` still uses the pre-rename `:content-cache-ttl` keyword, and `tests/tests-mcp.lisp:2318-2324` still calls the removed `chatbot-content-cache-ttl` accessor even though `src/core/data.lisp:186-188` defines `chatbot-content-cache-ttl-seconds`. | Warning noise reduces trust in routine load/test output and makes real regressions harder to spot. More importantly, it means tests are partially validating stale APIs rather than the current public surface. | Fix stale test call sites and assertions to use the `-seconds` API consistently, then keep test-system load warning-light as a standing maintenance rule. |

## Recently improved, but not finished

These areas should not be treated as fresh debt, but as **partially retired debt** that still has follow-through work left:

1. **Pure chat loop migration**
   - The normalized turn-result flow and thinner `chat` shell were a major improvement.
   - Conversation requirement and planner routing now live in `src/core/chat-routing.lisp` instead of being mixed into the public entry shell or main dispatch file.
   - Backend selection for `dispatch-chat-turn` now flows through focused backend-invocation helpers in `src/core/chat.lisp` instead of one inline case form.
   - `chat-turn` now packages file attachment preparation, generation-config resolution, pruning/cloning, and prompt-model override through focused preparation helpers before dispatch.
   - Public chat entry context resolution and active-conversation shell binding now live in `src/core/chat-entry.lisp`, leaving `src/core/chat.lisp` focused on turn preparation and backend dispatch before applying/checkpointing the turn result.
   - Post-turn compression and checkpoint persistence now also route through `finalize-chat-turn-result` in `src/core/conversations.lisp` instead of living inline in `src/core/chat.lisp`.
   - The old generic legacy-global mirroring layer in `src/core/vars.lisp` was also reduced to a dedicated `*default-conversation*` compatibility path plus explicit default-context compatibility, no-arg function seams now prefer the active explicit runtime context instead of ambient specials, `current-default-conversation` now also treats the default runtime context as canonical and only syncs the legacy special around that compatibility path, unrelated runtime-context resolution paths such as `new-chat`, persona runtime-context shaping, startup-chatbot initialization, agentic-loop fallback, and public chat entry-context selection no longer sync legacy `*default-conversation*` into the canonical runtime context, runtime function-seam and approval helpers no longer sync legacy `*default-conversation*` just to read or write unrelated seams, non-chat `call-with-runtime-context` opt-out paths no longer persist legacy function/approval seam overrides back into the canonical default runtime context, explicit `call-with-runtime-context` execution no longer rebinds the legacy `*default-conversation*` special, the public `chat` entry now re-enters the resolved conversation's runtime context before running the turn, transient active-conversation/planner setters and getters no longer mirror or consult legacy globals for explicit contexts, bot tool execution now enters the bot runtime context before invoking built-in or MCP tools, deprecated ambient aliases now live in `src/core/runtime-compatibility.lisp`, and `make-runtime-context` now inherits default conversations from the canonical runtime context instead of the deprecated ambient global.
   - Remaining debt: compatibility routing still sits close to the public entry shell, even though planner routing and dispatch concerns now live in separate helpers.

2. **Agentic loop and minion functional cleanup**
   - Loop-step state and minion restoration are clearer than before.
   - Agentic-loop startup shaping now also lives in a dedicated `src/orchestration/agentic-loop-startup.lisp` module, so startup instruction policy, history trimming, isolated startup sterility, and loop execution-profile selection are no longer interleaved with worker-thread and monitor-thread control flow.
   - Agentic loop supervision now also enforces watchdog-style timeout, failure, and zombie recovery through capped restarts, restart backoff, and last-safe-snapshot restoration in the monitor path.
   - Planner/minion orchestration now also journals agentic task results by immutable input so duplicate `spawnMinion` and `promptSubordinate` executions reuse the first completed result instead of reapplying side effects.
   - Agentic loops and subordinate prompts now also require strict validated JSON control envelopes, so the imperative shell no longer drives retries or spawns from free-form prefixes or regex-matched prose.
   - Remaining debt: orchestration still depends on global registries, threads, and runtime-context bridging, but the startup-policy slice is now narrower and easier to evolve independently.

3. **Reader vs accessor cleanup**
   - Immutable CLOS slots were tightened in several classes.
   - Remaining debt: large classes and lifecycle-heavy modules still expose broad mutable surfaces overall.

4. **MCP startup/config separation**
   - MCP configuration discovery, parsing, and built-in definition lookup now live in `src/mcp/mcp-config.lisp`, and `src/mcp/mcp-startup.lisp` is narrower than before.
   - MCP launch setup in `src/mcp/mcp-lifecycle.lisp` now also routes through smaller helpers for launch arguments, environment normalization, process launch, and supervision startup.
   - MCP server shutdown now also tracks stderr threads explicitly, routes stream closure / supervision-thread stop / process stop / final cleanup through focused helpers, and prefers graceful process termination before escalating through isolated forced thread/process termination helpers.
   - MCP server startup now also cleans up launched subprocesses and streams when supervision setup fails partway through.
   - MCP shutdown now also aborts pending request mailboxes immediately, so in-flight callers fail fast when a server is stopping instead of waiting for request timeouts.
   - MCP reader shutdown now also aborts pending request mailboxes when the reader loop exits on EOF or error, so unexpected server death also fails in-flight callers fast instead of leaving them to time out.
   - Shared startup-bot creation/reuse, startup-entry definition validation plus launch/cleanup shaping, empty-startup handling, config/status/apply/log strict-startup policy, shared-startup reference resolution, owned-server shutdown, and final startup-reference cleanup now also route through focused helpers in `src/mcp/mcp-startup.lisp`.
   - Remaining debt: process supervision, shutdown behavior, and startup-policy coupling still need further lifecycle decomposition.

5. **Tool-execution filesystem split**
   - Built-in tool registration and built-in dispatch now live in `src/mcp/builtin-dispatch.lisp`.
   - Built-in tool registration forms now also live in `src/mcp/builtin-registrations.lisp`.
   - MCP server lookup and MCP tool result-shaping now live in `src/mcp/mcp-dispatch.lisp`.
   - Built-in tool argument normalization now lives in `src/mcp/tool-arguments.lisp`, and filesystem tool helpers now live in `src/mcp/filesystem-tools.lisp`.
   - The remaining built-in filesystem handlers (`readFileLines`, `directory`, `writeFile`, and `deleteFile`) now also live in `src/mcp/filesystem-tools.lisp`.
   - System-instruction and sampling tool helpers now also live in `src/mcp/chatbot-state-tools.lisp`.
   - Eval and grounding helpers now also live in `src/mcp/eval-grounding-tools.lisp`.
   - Git tool behavior now also lives in `src/mcp/git-tools.lisp`.
   - Agentic loop built-ins now also live in `src/mcp/agentic-loop-tools.lisp`.
   - Planner and minion orchestration now also live in `src/mcp/planner-minion-tools.lisp`.
   - Remaining debt: `src/mcp/tool-execution.lisp` is now much narrower, and mostly holds top-level tool invocation helpers.

6. **Provider backend phase splitting**
   - `src/backends/backend-google.lisp` now separates Google request submission into API-key validation, request assembly, HTTP posting, and HTTP-response normalization helpers above `submit-google-turn`.
   - Google tool-recursion shaping now also routes through focused helper functions instead of one large inline lambda cluster inside `chat-google`.
   - `src/backends/backend-openai.lisp` now separates OpenAI-compatible request submission into API-key validation, request assembly, streaming request posting, and stream-consumption setup helpers above `submit-openai-turn`.
   - OpenAI-compatible tool-recursion shaping now also routes through focused helper functions instead of one large inline lambda cluster inside `chat-openai`.
   - `src/backends/backend-gemini.lisp` now separates Gemini Interactions submission into API-key validation, request assembly, streaming request posting, and stream-consumption setup helpers above `submit-gemini-turn`.
   - Gemini tool-recursion shaping now also routes through focused helper functions instead of one large inline continuation block inside `chat-gemini`.
   - Remaining debt: provider modules still keep recursion shaping inside provider files, but the main inline continuation clusters are now gone and submission phases are consistently thinner than before.

7. **Round-robin orchestration state machine**
   - `src/orchestration/round-robin.lisp` now drives participant scheduling through an explicit session FSM with pure transition helpers and a thin shell that applies the resulting state and runs the selected participant turn.
   - Remaining debt: planner/minion delegation still uses more implicit orchestration than round-robin and should eventually adopt the same explicit state-transition discipline.

8. **Minion runtime-state isolation**
   - Live minion checkpoint writes now default to `CHATBOT_MINIONS_DATA_DIR` when configured, otherwise to `~/.chatbot/data/minions/`, so ordinary runtime activity no longer writes state into the tracked repository tree by default.
   - Checkpoint-oriented tests now bind explicit temporary minion-state directories instead of depending on ambient repository paths.
   - Remaining debt: legacy tracked JSON state files still need cleanup or reclassification as fixtures.

## Suggested fix order

1. **Retire the legacy-global/runtime-context bridge** enough that new work no longer depends on it.
2. **Refactor checkpoint persistence and restore into a shared schema layer** so new state fields stop requiring parallel manual edits.
3. **Refactor MCP lifecycle and startup/shutdown mechanics** into smaller, testable layers.
4. **Break up the largest orchestration hotspots** in `agentic-loops.lisp` and `conversations.lisp`.
5. **Remove duplicate runtime trees and tracked state artifacts** so the repository shape becomes easier to trust.
6. **Clean warning noise and split giant tests** to improve signal before deeper refactors.
7. **Deepen lifecycle and concurrency tests** after the high-risk modules are decomposed.

## Current snapshot summary

The latest repo scan reinforces that the biggest remaining debt is **structural concentration**: a few hotspot orchestration files, duplicated persistence mapping, tracked runtime artifacts, and a mirrored sandbox tree still raise the cost and risk of otherwise modest changes.
